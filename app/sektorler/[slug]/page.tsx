import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {sectors,type SectorSlug} from '@/lib/sectors';import {PageIntro,Deliverables,CTA} from '@/components/agency-content';
export function generateStaticParams(){return Object.keys(sectors).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=sectors[slug as SectorSlug];return p?{title:`${p.title} | Aymek Dijital`,description:p.problem}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=sectors[slug as SectorSlug];if(!p)notFound();return <><PageIntro label={p.title.toLocaleUpperCase('tr-TR')} title={p.headline} description={p.problem} back={{href:'/sektorler',label:'Tüm sektörler'}}/><section className="wrap detail-split"><div><p className="eyebrow">SEKTÖREL YAKLAŞIM</p><h2>Müşterinizi anlar,<br/>planı ona göre kurarız.</h2><p>İletişim kuralları, hedef bölge, satış döngüsü ve mevcut dijital varlıklar analiz edilir. Kanalların uygunluğu proje başlangıcında kontrol edilir.</p></div><Deliverables items={p.solutions}/></section><CTA/></>}

import type {Metadata} from 'next';import {ArrowUpRight} from 'lucide-react';import {PageIntro} from '@/components/agency-content';import ContactBrief from '@/components/contact-brief';import {brand} from '@/lib/brand';
export const metadata:Metadata={title:'İletişim | Aymek Dijital',description:'Aymek Dijital ile projenizi konuşun. Telefon, WhatsApp ve e-posta üzerinden ulaşın.'};
export default async function Page({searchParams}:{searchParams:Promise<{hizmet?:string}>}){const query=await searchParams;return <><PageIntro label="İLETİŞİM" title="Bir merhaba, yeni bir başlangıç." description="Aklınızda bir proje, geliştirmek istediğiniz bir marka veya yalnızca bir soru varsa konuşalım."/><section className="wrap contact-grid"><div className="contact-details"><p className="eyebrow">DOĞRUDAN ULAŞIN</p><a href="tel:+905421298316">{brand.phone}</a><a href="tel:+905431298316">{brand.phone2}</a><a href={`mailto:${brand.email}`}>{brand.email}</a><p className="eyebrow">OFİSİMİZ</p><p style={{marginTop:18}}>{brand.address}</p><a href={`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(brand.address)}`} target="_blank" rel="noreferrer" className="text-link" style={{fontSize:13}}>Haritada aç <ArrowUpRight size={16}/></a><p className="eyebrow">İLK GÖRÜŞME</p><p style={{marginTop:18}}>İhtiyacınızı dinleyelim, mevcut durumunuzu değerlendirelim ve birlikte uygulanabilir bir başlangıç planı oluşturalım.</p></div><ContactBrief initialService={query.hizmet}/></section></>}

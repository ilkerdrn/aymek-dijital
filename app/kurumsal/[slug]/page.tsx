import type {Metadata} from 'next';import {notFound} from 'next/navigation';import {corporatePages,type CorporateSlug} from '@/lib/corporate';import {PageIntro,CTA} from '@/components/agency-content';
export function generateStaticParams(){return Object.keys(corporatePages).map(slug=>({slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const p=corporatePages[slug as CorporateSlug];return p?{title:`${p.title} | Aymek Dijital`,description:p.intro}:{}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const p=corporatePages[slug as CorporateSlug];if(!p)notFound();return <><PageIntro label={p.title.toLocaleUpperCase('tr-TR')} title={p.headline} description={p.intro} back={{href:'/kurumsal',label:'Ajansı keşfedin'}}/><section className="wrap article-body">{p.sections.map((x,i)=><section key={x.title}><p className="eyebrow">0{i+1}</p><h2 style={{marginTop:16}}>{x.title}</h2><p>{x.text}</p></section>)}<div className="label-list">{p.values.map(v=><span key={v}>{v}</span>)}</div></section><CTA/></>}

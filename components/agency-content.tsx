import {ArrowUpRight,ArrowLeft,Check} from 'lucide-react';
import {referenceIds,referenceImage,referenceNames} from '@/lib/brand';
export function PageIntro({label,title,description,back}:{label:string;title:string;description?:string;back?:{href:string;label:string}}){return <section className="page-intro wrap">{back&&<a className="back-link" href={back.href}><ArrowLeft size={15}/>{back.label}</a>}<p className="eyebrow"><span/>{label}</p><h1>{title}</h1>{description&&<p className="intro-text">{description}</p>}</section>}
export function CTA(){return <section className="cta-band wrap"><p className="eyebrow">BİR SONRAKİ ADIM</p><div><h2>Markanızın yeni<br/>hikâyesini yazalım.</h2><a href="/iletisim" className="pill brand">Projenizi konuşalım <ArrowUpRight size={20}/></a></div></section>}
export function ContentCard({title,text,href,index}:{title:string;text:string;href:string;index:number}){return <a className="content-card" href={href}><span className="card-index">{String(index+1).padStart(2,'0')}<ArrowUpRight size={25}/></span><h2>{title}</h2><p>{text}</p><span className="text-link">Detayları keşfet <ArrowUpRight size={16}/></span></a>}
export function ReferenceGrid({limit}:{limit?:number}){const ids=limit?referenceIds.slice(0,limit):referenceIds;return <div className="reference-grid">{ids.map((id,i)=><div key={id} className="reference-logo"><img src={referenceImage(id)} alt={referenceNames[id]||`Aymek Dijital referans markası ${i+1}`} width="300" height="275" loading="lazy"/></div>)}</div>}
export function Deliverables({items}:{items:readonly string[]}){return <div className="deliverables">{items.map((x,i)=><div key={x}><small>{String(i+1).padStart(2,'0')}</small><Check size={20}/><h3>{x}</h3></div>)}</div>}
